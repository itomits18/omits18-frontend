import AuthBg from '../(container)/AuthBg';

export default function page() {
  return (
    <section className="min-h-screen max-w-full bg-gradient-to-b from-white via-blue-100 via-20% to-green-200">
      <AuthBg />
    </section>
  );
}
